# Friend Pocket

GitHub Pages standalone build. The main public site is `index.html` and its CSS/JS are embedded so it works even if GitHub Pages has a path/cache issue.

Upload the contents of this folder to the repository root.
